<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Adding My Own Custom Style -->
    <style name="myCustomStyle"
        parent="@android:style/TextAppearance.Medium"> <!-- Inheriting the settings of one of the pre-existing styles that comes with Android SDK-->
        <!-- Name attrib is the same as the attrib used in Layout File -->
        <!-- Name attrib typically starts with android: prefix -->
        <!-- The device will select whatever monospace font that is available; usually it is Courier -->
        <item name="android:typeface">monospace</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#0000FF</item>
    </style>

</resources>
